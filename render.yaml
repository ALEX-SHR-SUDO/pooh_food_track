services:
  # Backend SMS Server
  - type: web
    name: pooh-food-sms-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 25.6.0
      - key: PORT
        generateValue: true
